import { motion } from "framer-motion"

export const LoginForm = () => {
    return (
         <div>
            <motion.div>
                nome
            </motion.div>
            <motion.div>
                email
            </motion.div>
            <motion.div>
                password
            </motion.div>
         </div>
    )
}
